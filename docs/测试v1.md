# 力量训练记录 App — 测试 Codex Prompt 集

> 前置条件：所有功能代码已实现（B0-B7 后端 + F0-F8 前端）
> 后端测试栈：Go testing + testify + httptest + testcontainers-go + k6
> 前端测试栈：Jest + @testing-library/react-native + Detox

---

## 🧪 Prompt T-B1: 后端认证模块测试

```
基于已有的 Go 后端代码，为认证模块编写完整的单元测试和集成测试。

测试文件：`internal/service/auth_service_test.go` + `internal/handler/auth_handler_test.go`

### 测试工具配置
- 使用 testify/assert + testify/require + testify/suite
- 使用 testcontainers-go 启动 PostgreSQL + Redis 容器（集成测试）
- 创建 `internal/testutil/setup.go`：
  - SetupTestDB(): 启动 PostgreSQL testcontainer，AutoMigrate，返回 *gorm.DB
  - SetupTestRedis(): 启动 Redis testcontainer，返回 *redis.Client
  - TearDown(): 清理容器
  - CreateTestUser(db, email, password): 创建测试用户并返回
  - GetAuthToken(userId): 生成有效 JWT token 供其他测试使用

### Service 层单元测试 (auth_service_test.go)
使用真实数据库（testcontainers），不 mock repository：

```go
func TestRegister_Success(t *testing.T)
// 合法 email + password(8位+)，返回 AuthResponse，包含 access_token, refresh_token, user info
// 验证：数据库中用户已创建，密码为 bcrypt 哈希（非明文），Redis 中存在 refresh token

func TestRegister_DuplicateEmail(t *testing.T)
// 先注册一个用户，再用相同 email 注册，返回错误，用户数量不变

func TestRegister_WeakPassword(t *testing.T)
// 密码 < 8 位，返回参数校验错误

func TestRegister_InvalidEmail(t *testing.T)
// email 格式非法（如 "notanemail"），返回参数校验错误

func TestLogin_Success(t *testing.T)
// 先注册，再用正确凭证登录，返回新 token 对

func TestLogin_WrongPassword(t *testing.T)
// 错误密码，返回认证失败，不暴露"用户是否存在"

func TestLogin_NonExistentUser(t *testing.T)
// 不存在的 email，返回认证失败，错误信息与密码错误一致

func TestRefreshToken_Success(t *testing.T)
// 合法 refresh_token，返回新 token 对，旧 refresh_token 在 Redis 中删除

func TestRefreshToken_Expired(t *testing.T)
// 过期的 refresh_token，返回 401

func TestRefreshToken_Revoked(t *testing.T)
// 从 Redis 中手动删除 refresh_token 后尝试刷新，返回 401

func TestChangePassword_Success(t *testing.T)
// 旧密码正确，新密码更新成功，旧密码无法登录，新密码可以登录

func TestChangePassword_WrongOldPassword(t *testing.T)
// 旧密码错误，返回错误，密码未变更

func TestUpdateProfile_Success(t *testing.T)
// 更新 nickname/weight_unit/language，返回更新后的 user info
```

### Handler 层集成测试 (auth_handler_test.go)
使用 httptest + gin.TestMode，发送真实 HTTP 请求：

```go
func TestRegisterHandler(t *testing.T)
// POST /api/v1/auth/register，body: {email, password, nickname}
// 验证：status 201, response body 包含 access_token, user.email 正确

func TestLoginHandler(t *testing.T)
// POST /api/v1/auth/login
// 验证：status 200, response 包含 token 对

func TestProfileHandler_Unauthorized(t *testing.T)
// GET /api/v1/auth/profile 无 Authorization header
// 验证：status 401

func TestProfileHandler_InvalidToken(t *testing.T)
// GET /api/v1/auth/profile 携带篡改的 JWT
// 验证：status 401

func TestProfileHandler_Success(t *testing.T)
// GET /api/v1/auth/profile 携带有效 token
// 验证：status 200, 返回正确用户信息
```

每个测试函数必须独立（不依赖执行顺序），使用 t.Cleanup 清理测试数据。
运行命令：`go test ./internal/... -v -count=1 -race`
```

---

## 🧪 Prompt T-B2: 后端动作库模块测试

```
为动作库模块编写完整测试。

测试文件：`internal/service/exercise_service_test.go` + `internal/handler/exercise_handler_test.go`

### Service 层测试

```go
func TestListExercises_NoFilter(t *testing.T)
// seed 预设动作 + 创建自定义动作
// 结果：返回系统预设（user_id=null）+ 当前用户自定义（user_id=currentUser）
// 不返回其他用户的自定义动作，不返回 is_archived=true 的动作

func TestListExercises_FilterByMuscle(t *testing.T)
// muscle=chest，仅返回 primary_muscles 包含 "chest" 的动作
// 使用 PostgreSQL 的 ANY() 操作符查询 text[] 数组

func TestListExercises_FilterByEquipment(t *testing.T)
// equipment=barbell，仅返回杠铃动作

func TestListExercises_FilterByCategory(t *testing.T)
// category=strength，仅返回力量动作

func TestListExercises_SearchChinese(t *testing.T)
// search=卧推，返回名称包含"卧推"的动作，ILIKE 模糊匹配

func TestListExercises_SearchEnglish(t *testing.T)
// search=bench，返回 name_en 包含 "bench" 的动作

func TestListExercises_Pagination(t *testing.T)
// page=1&page_size=10，验证返回 10 条 + total 正确
// page=2 返回下一页数据，与第一页无重叠

func TestCreateExercise_Success(t *testing.T)
// 完整字段：name, category, primary_muscles, equipment, tracking_type
// 验证：is_custom=true, user_id=当前用户, 可在列表中查到

func TestCreateExercise_MissingRequired(t *testing.T)
// 缺少 name，返回 400 参数校验错误

func TestCreateExercise_InvalidEnum(t *testing.T)
// category="invalid"，返回 400

func TestUpdateExercise_OwnCustom(t *testing.T)
// 更新自己的自定义动作名称，成功

func TestUpdateExercise_SystemExercise(t *testing.T)
// 尝试更新系统预设动作，返回 403

func TestUpdateExercise_OtherUserExercise(t *testing.T)
// 尝试更新其他用户的动作，返回 404（不暴露存在性）

func TestDeleteExercise_SoftDelete(t *testing.T)
// 删除自定义动作，is_archived=true，列表不再返回
// 但数据库记录仍存在（软删除），已引用该动作的训练记录不受影响

func TestDeleteExercise_SystemExercise(t *testing.T)
// 尝试删除系统动作，返回 403

func TestSeedExercises_Idempotent(t *testing.T)
// 执行两次 seed，动作数量不翻倍（幂等）
// 验证 seed 后动作数 >= 150
```

### Handler 层集成测试
对每个 API 端点发送 HTTP 请求，验证 status code + response body 结构。
重点测试：未认证访问返回 401，参数校验返回 400 并包含具体错误信息。
```

---

## 🧪 Prompt T-B3: 后端训练计划模块测试

```
为训练计划模块编写完整测试。

测试文件：`internal/service/plan_service_test.go`

### 测试用例

```go
func TestCreatePlan_Success(t *testing.T)
// 创建计划（name + goal），验证 ID 生成，is_active 默认 false

func TestGetPlanDetail_NestedPreload(t *testing.T)
// 创建计划 → 添加 2 个 Day → 每个 Day 添加 3 个动作
// 获取详情，验证返回完整的 Plan → Days → PlanExercises → Exercise 四层嵌套
// Exercise 信息（名称、肌群等）正确填充

func TestAddDay_SortOrder(t *testing.T)
// 依次添加 3 个 Day，sort_order 自动递增 0, 1, 2

func TestAddExerciseToDay(t *testing.T)
// 向某 Day 添加动作，指定 target_sets=4, target_reps="8-12", rest_seconds=90
// 验证字段正确存储

func TestReorderExercises(t *testing.T)
// 3 个动作 [A, B, C]，重排为 [C, A, B]
// 验证 sort_order 更新，重新查询顺序正确

func TestReorderExercises_InvalidIDs(t *testing.T)
// 传入不属于该 Day 的 ID，返回错误

func TestDuplicatePlan_DeepCopy(t *testing.T)
// 复制包含 2 Day × 3 Exercise 的计划
// 验证：新计划 ID 不同，所有 Day ID 不同，所有 PlanExercise ID 不同
// 修改副本的动作不影响原始计划
// 名称自动追加 "(副本)"

func TestSetActivePlan_MutualExclusion(t *testing.T)
// 创建 3 个计划，激活第 2 个
// 验证：第 2 个 is_active=true，其余 is_active=false
// 再激活第 3 个，验证只有第 3 个为 true

func TestDeletePlan_CascadeDelete(t *testing.T)
// 删除计划，验证关联的 Days 和 PlanExercises 全部级联删除
// 动作库中的 Exercise 不受影响

func TestAccessOtherUserPlan_NotFound(t *testing.T)
// 用户 A 创建计划，用户 B 尝试访问，返回 404

func TestSuperset_GroupExercises(t *testing.T)
// 3 个动作，前 2 个 superset_group=1，第 3 个无
// 获取详情，验证 superset_group 字段正确
```
```

---

## 🧪 Prompt T-B4: 后端训练记录模块测试

```
为训练记录模块编写完整测试，这是最关键的测试。

测试文件：`internal/service/workout_service_test.go` + `internal/repository/workout_repo_test.go`

### 核心测试用例

```go
// === 创建与查询 ===

func TestCreateWorkout_FullData(t *testing.T)
// 创建包含 3 个动作、每个动作 4 组的完整训练
// 验证：workout, workout_exercises, workout_sets 三张表数据全部写入
// total_volume 计算正确（排除 warmup 组）
// total_sets 计算正确

func TestGetWorkoutDetail_NestedPreload(t *testing.T)
// 获取训练详情，验证 workout → exercises → sets 完整嵌套
// exercise 信息（名称等）正确填充

func TestListWorkouts_DateFilter(t *testing.T)
// 创建 3 天的训练，按日期范围筛选
// date_from + date_to 只返回范围内的训练

func TestListWorkouts_ExerciseFilter(t *testing.T)
// 按特定动作筛选，仅返回包含该动作的训练

func TestListWorkouts_Pagination(t *testing.T)
// 按日期降序分页，验证分页正确

// === 批量同步（核心） ===

func TestBatchSync_SingleWorkout(t *testing.T)
// 同步 1 条完整训练（含 exercises + sets）
// 验证：数据完整写入，返回 client_id → server_id 映射

func TestBatchSync_MultipleWorkouts(t *testing.T)
// 同步 10 条训练
// 验证：全部成功写入，事务一致性（要么全成功要么全失败）

func TestBatchSync_Idempotent(t *testing.T)
// 同一批数据同步两次（相同 client_id）
// 第二次不创建重复记录，返回已存在的 server_id

func TestBatchSync_ExceedLimit(t *testing.T)
// 提交 51 条训练（超过 50 条限制），返回 400

func TestBatchSync_TransactionRollback(t *testing.T)
// 批量同步中某条数据有非法外键（exercise_id 不存在）
// 整个事务回滚，所有数据都不写入

// === 容量计算 ===

func TestVolumeCalculation_ExcludeWarmup(t *testing.T)
// 4 组：1 组 warmup(60kg×10) + 3 组正式(100kg×10)
// total_volume = 3000（不含 warmup 的 600）

func TestVolumeCalculation_MixedUnits(t *testing.T)
// 有的组用 kg 有的用 lbs，volume 统一按各自单位计算
// （容量只做简单求和，不做单位转换）

func TestVolumeCalculation_IncompleteSet(t *testing.T)
// 未完成的组(is_completed=false)不计入容量

// === 删除 ===

func TestDeleteWorkout_CascadeDelete(t *testing.T)
// 删除训练，验证关联的 workout_exercises 和 workout_sets 全部级联删除

func TestDeleteOtherUserWorkout_NotFound(t *testing.T)
// 尝试删除其他用户的训练，返回 404
```
```

---

## 🧪 Prompt T-B5: 后端数据分析模块测试

```
为数据分析模块编写完整测试。所有聚合查询必须用真实 PostgreSQL 测试。

测试文件：`internal/service/stats_service_test.go`

### 测试数据准备
创建 `internal/testutil/fixtures.go`：
- SeedStatsTestData(db): 生成 8 周的模拟训练数据
  - 每周 4 次训练，每次 5 个动作，每动作 4 组
  - 涵盖所有主要肌群
  - 第 3 周有一天缺失（用于测试 streak 断裂）
  - 最后一次训练包含一个 PR

### 测试用例

```go
func TestGetDashboard(t *testing.T)
// 验证所有字段：weekly_workouts, weekly_volume, last_week_volume,
// volume_change_percent, current_streak, recent_prs, muscle_distribution
// volume_change_percent 计算公式：(本周-上周)/上周 * 100

func TestGetDashboard_NoData(t *testing.T)
// 新用户无训练数据，Dashboard 返回全零，不报错

func TestGetDashboard_RedisCache(t *testing.T)
// 第一次调用：查数据库，写入 Redis
// 第二次调用：直接从 Redis 返回（可通过 mock 计时器或检查数据库查询次数验证）
// 提交新训练后：缓存失效，下次调用重新查数据库

func TestGetVolumeHistory_Weekly(t *testing.T)
// period=weekly，date_from/to 覆盖 8 周
// 返回 8 个数据点，每个 volume 值为该周所有训练容量之和

func TestGetVolumeHistory_Daily(t *testing.T)
// period=daily，验证按天聚合

func TestGetVolumeHistory_Monthly(t *testing.T)
// period=monthly，验证按月聚合

func TestGetVolumeHistory_EmptyRange(t *testing.T)
// 日期范围内无训练，返回空数组

func TestGetMuscleDistribution(t *testing.T)
// 验证各肌群容量分布
// 注意：一个动作有多个 primary_muscles 时，容量会被分配到多个肌群（UNNEST）

func TestGetFrequencyStats_Streak(t *testing.T)
// 连续 5 天训练 → streak=5
// 断了 1 天后又练了 2 天 → current_streak=2, longest_streak=5

func TestGetFrequencyStats_ByDayOfWeek(t *testing.T)
// 验证 by_day_of_week 返回各星期几的训练次数分布

func TestGetFrequencyStats_ByTimeOfDay(t *testing.T)
// 创建早上(6-12)、下午(12-18)、晚上(18-24)的训练
// 验证 by_time_of_day 分类正确

func TestGetExerciseStats(t *testing.T)
// 某动作 8 次训练历史
// volume_history: 8 个数据点
// max_weight_history: 8 个数据点
// estimated_1rm: 基于最近一次最佳组计算
// personal_records: 包含该动作的所有 PR

func TestStatsQueryPerformance(t *testing.T)
// 在 500 次训练 × 5 动作 × 4 组的数据量下
// 每个聚合查询耗时 < 200ms
// 使用 testing.B benchmark 或 time.Since 计时
```
```

---

## 🧪 Prompt T-B6: 后端 V1.1-V3.0 功能测试

```
为 V1.1-V3.0 版本的后端功能编写测试。

### PR 模块测试 (pr_service_test.go)

```go
func TestCheckPR_NewMaxWeight(t *testing.T)
// 历史最大 100kg，当前组 105kg，应创建 max_weight PR 记录

func TestCheckPR_NoRecord(t *testing.T)
// 历史最大 100kg，当前组 95kg，不创建 PR

func TestCheckPR_NewEstimated1RM(t *testing.T)
// 当前组的 e1RM 超过历史最佳 e1RM，创建 estimated_1rm PR

func TestCheckPR_FirstWorkout(t *testing.T)
// 该动作第一次训练，自动创建 PR（首次即最佳）

func TestGetPRHistory(t *testing.T)
// 验证按时间降序返回，按动作筛选正确
```

### 模板模块测试 (template_service_test.go)

```go
func TestGetBuiltInTemplates(t *testing.T)
// 返回 8-10 个内置模板，每个包含完整 Day+Exercise 快照

func TestApplyTemplate_CreatePlan(t *testing.T)
// 从模板创建计划，验证计划数据完整
// 新计划独立于模板（修改计划不影响模板）

func TestSaveAsTemplate(t *testing.T)
// 从计划保存为模板，Snapshot 包含完整 Days+Exercises

func TestExportImportTemplate(t *testing.T)
// 导出为 JSON → 导入 → 验证数据一致
```

### 挑战模块测试 (challenge_service_test.go)

```go
func TestCreateChallenge(t *testing.T)
// 创建容量目标挑战，current_value=0，is_completed=false

func TestUpdateChallengeProgress_VolumeType(t *testing.T)
// 同步一次训练（容量 5000），挑战目标 50000
// current_value 更新为 5000

func TestChallengeCompletion(t *testing.T)
// current_value 累计达到 target_value
// is_completed 自动标记为 true

func TestChallengeExpired(t *testing.T)
// 截止日期已过但未完成，查询时标记为未完成
```

### 教练模块测试 (coach_service_test.go)

```go
func TestSendInvitation(t *testing.T)
// 教练发送邀请，invitation 记录 status=pending

func TestAcceptInvitation(t *testing.T)
// 会员接受，coach_client 关系建立，status=active

func TestRejectInvitation(t *testing.T)
// 会员拒绝，invitation status=rejected，无 coach_client 记录

func TestCoachViewClientWorkouts(t *testing.T)
// 教练查看自己会员的训练历史，数据正确
// 教练不能查看非自己会员的数据

func TestCoachAddComment(t *testing.T)
// 教练对会员训练添加评语，评语关联正确

func TestNonCoachAccessCoachAPI(t *testing.T)
// 普通用户（role=user）访问教练接口，返回 403

func TestCoachAccessUnrelatedClient(t *testing.T)
// 教练访问非自己会员的数据，返回 404
```
```

---

## 🧪 Prompt T-F1: 前端工具函数 + Store 测试

```
为前端工具函数和 Zustand Store 编写 Jest 单元测试。

### 工具函数测试 `src/utils/__tests__/`

文件：`calculateVolume.test.ts`
```typescript
describe('calculateVolume', () => {
  test('正常计算：3组(100kg×10) = 3000', () => {})
  test('排除热身组：1组warmup(60×10) + 3组(100×10) = 3000', () => {})
  test('空数组返回 0', () => {})
  test('未完成的组(isCompleted=false)不计入', () => {})
  test('weight=null 的组跳过', () => {})
  test('reps=null 的组跳过', () => {})
  test('混合：部分有效部分无效', () => {})
})
```

文件：`estimate1RM.test.ts`
```typescript
describe('estimate1RM', () => {
  test('Epley公式：100kg × 10reps = 133.33', () => {})
  test('Brzycki公式：100kg × 10reps = 133.33', () => {})
  test('Lombardi公式：100kg × 10reps', () => {})
  test('1 rep 返回原重量', () => {})
  test('0 reps 返回 0', () => {})
  test('负数输入抛出错误', () => {})
})
```

文件：`convertWeight.test.ts`
```typescript
describe('convertWeight', () => {
  test('kg→lbs：100kg = 220.46lbs', () => {})
  test('lbs→kg：225lbs = 102.06kg', () => {})
  test('相同单位转换返回原值', () => {})
  test('0 返回 0', () => {})
  test('精度：结果保留 2 位小数', () => {})
})
```

### Zustand Store 测试 `src/store/__tests__/`

文件：`workoutStore.test.ts`
```typescript
describe('workoutStore', () => {
  beforeEach(() => { /* 重置 store */ })

  describe('startWorkout', () => {
    test('初始化状态：elapsedSeconds=0, exercises=[], totalVolume=0', () => {})
    test('从计划开始：planDayId 设置正确', () => {})
    test('空白开始：planDayId=undefined', () => {})
  })

  describe('addExercise', () => {
    test('添加动作后 exercises 数组增加', () => {})
    test('新添加的动作包含 1 个空白组', () => {})
  })

  describe('updateSet', () => {
    test('更新 weight：值正确更新', () => {})
    test('更新 reps：值正确更新', () => {})
    test('更新后容量重新计算', () => {})
  })

  describe('completeSet', () => {
    test('标记 isCompleted=true', () => {})
    test('自动在末尾添加下一组空行', () => {})
    test('动作容量更新', () => {})
    test('总容量更新', () => {})
    test('totalSets 增加', () => {})
    test('warmup 组不增加 totalSets', () => {})
  })

  describe('removeSet', () => {
    test('删除组后容量重新计算', () => {})
    test('组号重新排列', () => {})
  })

  describe('removeExercise', () => {
    test('删除动作后总容量重新计算', () => {})
  })

  describe('finishWorkout', () => {
    test('计算最终 totalVolume 和 totalSets', () => {})
    test('warmup 组不计入 totalVolume', () => {})
    test('未完成组不计入', () => {})
  })
})
```

文件：`settingsStore.test.ts`
```typescript
describe('settingsStore', () => {
  test('默认值：weightUnit=kg, language=zh', () => {})
  test('切换 weightUnit 到 lbs', () => {})
  test('切换 language 到 en', () => {})
  test('设置 defaultRestSeconds', () => {})
})
```

所有测试必须独立，不依赖执行顺序。使用 jest.fn() mock 外部依赖（如 AsyncStorage、WatermelonDB）。
运行命令：`npx jest --coverage --verbose`
目标覆盖率：工具函数 100%，Store 90%+
```

---

## 🧪 Prompt T-F2: 前端组件测试

```
使用 @testing-library/react-native 为核心组件编写交互测试。

### 测试文件

文件：`src/components/__tests__/NumericKeypad.test.tsx`
```typescript
describe('NumericKeypad', () => {
  test('点击数字按钮更新显示值：依次点击 1-0-0 显示 100', () => {
    // render → fireEvent.press(1) → press(0) → press(0) → expect 显示 100
  })
  test('点击 +2.5：100 → 102.5', () => {})
  test('点击 -2.5：100 → 97.5', () => {})
  test('点击 +5：100 → 105', () => {})
  test('点击 -5：100 → 95', () => {})
  test('-2.5 不低于 0：当前 0，点击 -2.5 仍为 0', () => {})
  test('退格：123 → 12', () => {})
  test('清空：123 → 空', () => {})
  test('小数点：输入 67.5', () => {})
  test('不能输入两个小数点', () => {})
  test('确认按钮触发 onConfirm(value)', () => {})
  test('显示上次参考值（灰色小字）', () => {})
})
```

文件：`src/components/__tests__/RestTimer.test.tsx`
```typescript
describe('RestTimer', () => {
  test('初始显示预设时间', () => {})
  test('倒计时到 0 触发 onComplete', () => {
    // 使用 jest.useFakeTimers()
    // render(duration=90) → jest.advanceTimersByTime(90000) → expect onComplete called
  })
  test('+15s 按钮增加时间', () => {})
  test('-15s 按钮减少时间', () => {})
  test('-15s 不低于 0', () => {})
  test('跳过按钮触发 onSkip', () => {})
})
```

文件：`src/components/__tests__/SearchBar.test.tsx`
```typescript
describe('SearchBar', () => {
  test('输入文字后 300ms 触发 onSearch', () => {
    // jest.useFakeTimers()
    // 输入 "卧推" → advanceTimersByTime(300) → expect onSearch("卧推")
  })
  test('快速连续输入只触发一次（防抖）', () => {
    // 输入 "卧" → 100ms → "推" → advanceTimersByTime(300) → onSearch 只调用一次
  })
  test('清空按钮触发 onSearch("")', () => {})
  test('placeholder 显示正确', () => {})
})
```

文件：`src/components/__tests__/ExerciseCard.test.tsx`
```typescript
describe('ExerciseCard', () => {
  test('显示动作名称', () => {})
  test('显示肌群标签', () => {})
  test('显示器械图标', () => {})
  test('点击触发 onPress', () => {})
})
```

文件：`src/components/__tests__/PRCelebration.test.tsx`
```typescript
describe('PRCelebration', () => {
  test('显示 PR 类型和数值', () => {})
  test('2 秒后自动消失，触发 onDismiss', () => {
    jest.useFakeTimers()
    // render → advanceTimersByTime(2000) → expect onDismiss called
  })
  test('点击可提前关闭', () => {})
})
```

文件：`src/components/__tests__/MuscleHeatmap.test.tsx`
```typescript
describe('MuscleHeatmap', () => {
  test('传入数据后 SVG 正常渲染不崩溃', () => {})
  test('无数据时显示全灰色', () => {})
  test('高频肌群颜色深于低频', () => {
    // 通过检查 SVG 元素的 fill 属性验证
  })
})
```

运行命令：`npx jest --testPathPattern="components/__tests__" --verbose`
```

---

## 🧪 Prompt T-E1: E2E 测试 (Detox)

```
使用 Detox 编写核心用户流程的端到端测试。

### Detox 配置
- 测试 Release build（性能更接近真实）
- iOS Simulator: iPhone 15 Pro
- 每个测试前重置 App 数据

### 测试文件

文件：`e2e/workoutFlow.test.ts`
```typescript
describe('核心训练流程', () => {

  test('完整训练流程：计划开始→记录→完成', async () => {
    // 1. 创建计划：输入名称 "测试计划" → 添加 Day "胸日" → 添加动作 "杠铃卧推"
    // 2. 从计划开始训练：点击 "胸日"
    // 3. 记录第 1 组：输入重量 100 → 输入次数 10 → 点击完成
    // 4. 验证计时器自动启动
    // 5. 记录第 2 组：预填值显示 100/10 → 直接点击完成
    // 6. 点击结束训练
    // 7. 验证摘要页：总容量 2000，2 组
    // 8. 保存 → 验证历史列表出现该训练
  })

  test('空白训练：添加动作→记录→完成', async () => {
    // 1. 点击"空白开始"
    // 2. 添加动作"哑铃弯举"
    // 3. 记录 3 组
    // 4. 结束训练 → 验证保存成功
  })

  test('崩溃恢复', async () => {
    // 1. 开始训练 → 记录 2 组
    // 2. device.terminateApp() 杀掉 App
    // 3. device.launchApp() 重启
    // 4. 验证弹出恢复提示
    // 5. 点击恢复 → 验证 2 组数据完整
  })

  test('智能预填', async () => {
    // 1. 第一次训练：杠铃卧推 100kg × 10
    // 2. 结束训练
    // 3. 第二次训练：添加杠铃卧推
    // 4. 验证第一组预填值为 100 / 10
  })
})
```

文件：`e2e/planFlow.test.ts`
```typescript
describe('计划管理流程', () => {

  test('创建计划→添加Day→添加动作→保存', async () => {
    // 完整的计划创建流程
    // 验证计划列表显示新计划
  })

  test('从模板创建计划', async () => {
    // 进入模板页 → 选择 PPL 模板 → 应用
    // 验证计划创建成功，包含 3 个 Day
  })

  test('编辑计划→修改动作→保存', async () => {
    // 打开已有计划 → 添加一个动作 → 保存
    // 重新打开验证修改保留
  })
})
```

文件：`e2e/analyticsFlow.test.ts`
```typescript
describe('数据分析流程', () => {

  test('训练后 Dashboard 数据更新', async () => {
    // 完成一次训练 → 进入分析页
    // 验证本周训练次数 >= 1
    // 验证本周容量 > 0
  })

  test('训练历史列表显示', async () => {
    // 进入历史页 → 验证列表非空
    // 点击某条 → 验证详情页数据完整
  })
})
```

文件：`e2e/syncFlow.test.ts`
```typescript
describe('云同步流程', () => {

  test('注册→登录→数据同步', async () => {
    // 本地创建训练数据
    // 注册账号 → 登录
    // 等待同步完成（观察同步指示器）
    // 验证数据可在云端查到（通过 API 验证）
  })

  test('离线训练→恢复网络→自动同步', async () => {
    // 开启飞行模式
    // 完成一次训练
    // 关闭飞行模式
    // 等待自动同步
    // 验证云端有该训练数据
  })
})
```

运行命令：
- iOS: `npx detox test --configuration ios.sim.release`
- Android: `npx detox test --configuration android.emu.release`
```

---

## 🧪 Prompt T-P1: 性能基准测试

```
编写后端 API 压测脚本和前端性能测试配置。

### 后端 k6 压测脚本

文件：`loadtest/api_benchmark.js`
```javascript
import http from 'k6/http';
import { check, sleep } from 'k6';
import { Rate, Trend } from 'k6/metrics';

const BASE_URL = __ENV.API_URL || 'http://localhost:8080';
let authToken = '';

export const options = {
  scenarios: {
    // 场景 1：API 响应时间基准
    api_benchmark: {
      executor: 'constant-vus',
      vus: 50,
      duration: '30s',
    },
    // 场景 2：高并发压力测试
    stress_test: {
      executor: 'ramping-vus',
      startVUs: 0,
      stages: [
        { duration: '30s', target: 100 },
        { duration: '60s', target: 200 },
        { duration: '30s', target: 0 },
      ],
    },
  },
  thresholds: {
    // 性能基准阈值
    'http_req_duration{name:GET_exercises}':      ['p(95)<50'],
    'http_req_duration{name:GET_exercises_search}': ['p(95)<80'],
    'http_req_duration{name:GET_plan_detail}':     ['p(95)<100'],
    'http_req_duration{name:POST_sync_single}':    ['p(95)<200'],
    'http_req_duration{name:POST_sync_batch}':     ['p(95)<500'],
    'http_req_duration{name:GET_dashboard}':       ['p(95)<200'],
    'http_req_duration{name:GET_dashboard_nocache}': ['p(95)<500'],
    'http_req_duration{name:GET_volume_stats}':    ['p(95)<300'],
    'http_req_duration{name:GET_muscle_stats}':    ['p(95)<300'],
    'http_req_failed': ['rate<0.001'],  // 错误率 < 0.1%
  },
};

export function setup() {
  // 注册测试用户 + 登录获取 token
  // Seed 测试数据：500 训练 × 5 动作 × 4 组
  // 返回 { token, exerciseId, planId }
}

export default function(data) {
  const headers = { Authorization: `Bearer ${data.token}`, 'Content-Type': 'application/json' };

  // GET /exercises（列表）
  let r1 = http.get(`${BASE_URL}/api/v1/exercises?page=1&page_size=20`, { headers, tags: { name: 'GET_exercises' } });
  check(r1, { 'exercises 200': (r) => r.status === 200 });

  // GET /exercises?search=卧推
  let r2 = http.get(`${BASE_URL}/api/v1/exercises?search=卧推`, { headers, tags: { name: 'GET_exercises_search' } });
  check(r2, { 'search 200': (r) => r.status === 200 });

  // GET /plans/:id（详情含嵌套）
  let r3 = http.get(`${BASE_URL}/api/v1/plans/${data.planId}`, { headers, tags: { name: 'GET_plan_detail' } });
  check(r3, { 'plan detail 200': (r) => r.status === 200 });

  // POST /workouts/sync（单条）
  let syncBody = JSON.stringify({ workouts: [generateWorkoutData(data.exerciseId)] });
  let r4 = http.post(`${BASE_URL}/api/v1/workouts/sync`, syncBody, { headers, tags: { name: 'POST_sync_single' } });
  check(r4, { 'sync 200': (r) => r.status === 200 });

  // GET /stats/dashboard
  let r5 = http.get(`${BASE_URL}/api/v1/stats/dashboard`, { headers, tags: { name: 'GET_dashboard' } });
  check(r5, { 'dashboard 200': (r) => r.status === 200 });

  // GET /stats/volume
  let r6 = http.get(`${BASE_URL}/api/v1/stats/volume?period=weekly&date_from=2025-10-01&date_to=2026-03-30`, { headers, tags: { name: 'GET_volume_stats' } });
  check(r6, { 'volume 200': (r) => r.status === 200 });

  // GET /stats/muscles
  let r7 = http.get(`${BASE_URL}/api/v1/stats/muscles?date_from=2026-01-01&date_to=2026-03-30`, { headers, tags: { name: 'GET_muscle_stats' } });
  check(r7, { 'muscles 200': (r) => r.status === 200 });

  sleep(0.5);
}

function generateWorkoutData(exerciseId) {
  // 生成随机训练数据：5 动作 × 4 组
  // client_id 使用 UUID v4 确保不重复
}
```

运行命令：`k6 run --env API_URL=http://localhost:8080 loadtest/api_benchmark.js`

### 后端 Go Benchmark 测试

文件：`internal/repository/stats_repo_benchmark_test.go`
```go
func BenchmarkGetDashboard(b *testing.B) {
  // 在 500 训练数据量下 benchmark Dashboard 查询
  // 目标：< 200ms/op
}

func BenchmarkGetVolumeHistory_6Months(b *testing.B) {
  // 6 个月数据的容量趋势查询
  // 目标：< 300ms/op
}

func BenchmarkGetMuscleDistribution(b *testing.B) {
  // UNNEST 聚合查询 benchmark
  // 目标：< 300ms/op
}

func BenchmarkBatchSync_10Workouts(b *testing.B) {
  // 10 条训练批量写入 benchmark
  // 目标：< 500ms/op
}
```

运行命令：`go test ./internal/repository/ -bench=. -benchmem -count=5 | tee bench.txt && benchstat bench.txt`

### 前端性能测试配置

Flashlight 配置 `flashlight.config.json`：
```json
{
  "tests": [
    { "name": "app_cold_start", "target": "tti < 2000ms" },
    { "name": "workout_screen_fps", "target": "fps >= 58" },
    { "name": "exercise_list_scroll", "target": "fps >= 58" },
    { "name": "chart_render", "target": "duration < 500ms" }
  ]
}
```

手动性能验收清单（使用秒表 + 屏幕录制）：
1. 单组记录端到端时间：点击重量→输入→点击次数→输入→完成 ≤ 3s
2. 预填场景单组时间：仅点击完成按钮 ≤ 1s
3. 搜索响应：输入到结果更新 ≤ 100ms
4. 数字键盘响应：按钮点击到数值更新 ≤ 16ms（1帧）
```

---

## 执行顺序

| 顺序 | Prompt | 内容 | 预计用例数 |
|------|--------|------|-----------|
| 1 | T-B1 | 后端认证模块测试 | 18 |
| 2 | T-B2 | 后端动作库测试 | 16 |
| 3 | T-B3 | 后端训练计划测试 | 11 |
| 4 | T-B4 | 后端训练记录测试 | 12 |
| 5 | T-B5 | 后端数据分析测试 | 12 |
| 6 | T-B6 | 后端 V1.1-V3.0 测试 | 18 |
| 7 | T-F1 | 前端工具函数+Store | 30+ |
| 8 | T-F2 | 前端组件测试 | 20+ |
| 9 | T-E1 | E2E 端到端测试 | 12 |
| 10 | T-P1 | 性能基准测试 | 12 指标 |
| **合计** | | | **160+** |

> **发布红线**：P0 用例 100% 通过 · 后端覆盖率 ≥ 80% · 前端覆盖率 ≥ 75% · 所有性能指标达标 · 单组记录 ≤ 3 秒 · 崩溃恢复通过 · 无 P0 安全漏洞
